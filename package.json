{
  "name": "@dictadata/storage-etl",
  "version": "0.4.0",
  "description": "Command line ELT utility to copy, convert and transform data from distributed sources.",
  "author": "Drew O. Letcher",
  "license": "MIT",
  "repository": "github:dictadata/storage-etl",
  "main": "storage-etl.js",
  "scripts": {
    "debug": "set LOG_LEVEL=debug& node --inspect-brk --preserve-symlinks storage-etl.js scan test/config/config-scan-1.json",
    "test": "node storage-etl.js test/input.json test/output.csv test/transforms.json"
  },
  "bin": {
    "storage-etl": "./storage-etl.js"
  },
  "keywords": [
    "ETL",
    "transform",
    "json",
    "csv",
    "elasticsearch",
    "mysql"
  ],
  "dependencies": {
    "@dictadata/storage-junctions": "^0.4.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-node": "^9.1.0",
    "eslint-plugin-promise": "^4.2.1"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
